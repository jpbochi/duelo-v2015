#!/bin/sh -e

curl -sS -L https://github.com/docker/compose/releases/download/1.4.0/docker-compose-`uname -s`-`uname -m` > docker-compose
chmod +x docker-compose
mv docker-compose ./sh
export PATH="$PATH:./sh"
docker-compose --version

docker info
